{
  "name": "dialogflow-service",
  "scripts": {
    "start": "micro -l tcp://0.0.0.0:${PORT-3000}",
    "dev": "micro-dev"
  },
  "dependencies": {
    "dialogflow": "^0.3.0",
    "micro": "latest",
    "micro-cors": "^0.1.0"
  },
  "devDependencies": {
    "micro-dev": "^3.0.0"
  }
}
